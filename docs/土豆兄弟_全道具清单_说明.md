# 土豆兄弟全道具清单（标签化）说明

> 生成日期：2026-04-22

已生成以下文件：

- `docs/土豆兄弟_全道具标签化表.csv`：导入策划表首选（字段齐全）
- `docs/土豆兄弟_全道具标签化表.tsv`：制表符版本
- `docs/土豆兄弟_全道具按标签索引.md`：按 Tag 分组的查阅文档

字段说明：

- `Name`
- `Rarity`
- `Effects`
- `BasePrice`
- `Limit`
- `UnlockedBy`
- `Tags`

备注：

- 本次从 Brotato Wiki `Items` 页面主表自动解析得到 `231` 条唯一道具记录。
- 同一道具可有多个 `Tags`，在标签索引文档中会重复出现在多个分组，这是预期行为。
